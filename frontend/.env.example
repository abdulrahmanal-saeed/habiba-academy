# frontend/.env.example
# Copy to .env.local and fill in values
# NEVER commit .env.local to git

# API base URL — points to PHP backend
VITE_API_BASE_URL=https://mshabibanabil.com

# Environment
VITE_APP_ENV=development

# Web Push VAPID public key (safe to expose in frontend)
# Get from Core/.env VAPID_PUBLIC_KEY
VITE_VAPID_PUBLIC_KEY=BM9k8Cr3_oVw3InM0n3cwNboASGLJMQjufMfebhwUp540A-PFIlxduannj_ejvweIpFd61rUpi_b51cm-AW0DVs

# ⚠️  NEVER add these to frontend .env:
# DB_PASS, ANTHROPIC_API_KEY, TEACHER_PASSWORD, ZIINA_API_KEY
# VAPID_PRIVATE_PEM
# These stay in backend/.env only
